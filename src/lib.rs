#![forbid(unsafe_code)]
//! Authorize by transport: decides by rules on the transport identity: mechanism, class,
//! address; a transport-layer policy.
//!
//! Declared and not yet written: `architecture.toml` carries the maturity. When it
//! is, it implements `Authorizer` (ADR-0050).
